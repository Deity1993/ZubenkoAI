# Produktion: Diese Variablen setzen (z.B. export JWT_SECRET=...)
JWT_SECRET=dein-sicheres-geheimnis-min-32-zeichen
PORT=3000
# SIP Configuration for JsSIP WebSocket Registration
# Configure these variables in your .env.local file

# SIP Provider WebSocket URI (wss for secure, ws for plain)
# Example: wss://sip.provider.com:8089
VITE_SIP_WEBSOCKET=wss://your-sip-provider-ws-endpoint:port

# SIP Username (without sip: prefix)
# Example: 1234567
VITE_SIP_USERNAME=your-sip-username

# SIP Password
VITE_SIP_PASSWORD=your-sip-password

# SIP Registrar/Domain
# Example: sip.provider.com
VITE_SIP_REGISTRAR=your-sip-registrar

# SIP Port (usually 5060 for UDP/TCP, 5061 for TLS)
VITE_SIP_PORT=5060

# SIP Protocol (TCP, TLS, or WSS - WebSocket is handled via URI)
VITE_SIP_PROTOCOL=TCP

# Optional: Display name for outbound calls
VITE_SIP_DISPLAY_NAME=My App

# API Configuration
VITE_API_URL=http://localhost:3001